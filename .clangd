# 只有当没有 compile_commands.json 时才生效的备用选项
# 如果你有 CMake，这个会自动让位给 CMake 的配置
If:
  PathMatch: .*
CompileFlags:
  Compiler: g++
  Add: [-std=c++20]
Diagnostics:
  UnusedIncludes: Strict
  ClangTidy:
    Add: 
      - bugprone-* # 开启：潜在的 bug
      - performance-* # 开启：性能优化建议
      - modernize-use-nullptr  # 开启：建议使用 nullptr
      - modernize-deprecated-headers # 开启：建议使用现代头文件
      - modernize-use-override # 开启：建议使用 override 关键字
      - readability-simplify-boolean-expr # 开启：简化布尔表达式
      - readability-named-parameter # 开启：命名参数建议
    Remove: 
      - modernize-use-trailing-return-type # 关闭：你不喜欢的后置返回类型
      - readability-identifier-length # 关闭：不强制标识符长度
      - readability-magic-numbers # 关闭：不强制魔法数字检查
      - readability-isolate-declaration # 关闭：不强制单独声明
      - google-explicit-constructor # 关闭：不强制显式构造函数
      - bugprone-easily-swappable-parameters # 关闭：不强制参数顺序检查
      - performance-avoid-endl # 关闭：允许使用 endl